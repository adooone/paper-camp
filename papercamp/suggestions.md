## Night findings
- night: 2026-09-15 | check=dead-code | chunk=src/cli | file=src/cli/toolbar-assets.ts | line=11 | commit=96b35c6658af50d0d93589f53bda08b3275d48e6 | severity=normal | `toolbarDir` is exported but only ever called locally within the same file (as the default value for `serveToolbarAsset`'s `dir` param); no other file imports it, so the export is unused.
- night: 2026-09-15 | check=dead-code | chunk=src/cli | file=src/cli/night-pass.ts | line=69 | commit=96b35c6658af50d0d93589f53bda08b3275d48e6 | severity=normal | `spawnClaude` is exported but never imported anywhere else in the codebase (including its own test file); it's only referenced locally as the default fallback for `opts.spawnAgent`, so the export appears dead.
- night: 2026-09-15 | check=security | chunk=src/cli | file=src/cli/daemon-server.ts | line=308 | commit=96b35c6658af50d0d93589f53bda08b3275d48e6 | severity=high | The machine-update bearer token is compared with `!==` (non-constant-time), letting a network peer that passes the Host-trust check use timing differences to brute-force the token and trigger an npm install + restart.
- night: 2026-09-15 | check=security | chunk=src/cli | file=src/cli/tunnel.ts | line=44 | commit=96b35c6658af50d0d93589f53bda08b3275d48e6 | severity=high | startQuickTunnel writes cloudflared's config to a fixed, predictable path in the shared OS tmpdir instead of a securely created temp file, allowing a local symlink/pre-create race to redirect the write on multi-user machines.
- night: 2026-09-16 | check=bugs | chunk=src/app | file=src/app/features/plans/hooks/use-finding-fix-task.ts | line=62 | commit=cb73cd1ec0cd7b9c53b0f70d057b0ee93cadf628 | severity=high | The effect sets `handledEntryId.current = outcome.id` before `Promise.all(findings.map(dismissNightFinding))` resolves, and the chain has no `.catch()`. If any `dismissNightFinding` call rejects (e.g. an API/network error), the rejection is unhandled, navigation never happens, and because the guard is already set the effect will never retry for that outcome on a later render — the finding stays stuck in the report even though its fix task is marked 'done'.
- night: 2026-09-16 | check=performance | chunk=src/app | file=src/app/features/plans/views/night-report-section.tsx | line=108 | commit=cb73cd1ec0cd7b9c53b0f70d057b0ee93cadf628 | severity=normal | Each ChunkCard mounts useFindingFixTask, which fires loadTaskLog() on mount, so rendering N chunks in the review-findings grid issues N redundant task-log fetches instead of one shared load.
- night: 2026-09-16 | check=tests | chunk=src/app | file=src/app/features/plans/hooks/use-finding-fix-task.ts | line=62 | commit=cb73cd1ec0cd7b9c53b0f70d057b0ee93cadf628 | severity=normal | No test file exists for useFindingFixTask, so the auto-dismiss-on-done logic (dismissing every finding and navigating home only when a single finding's fix task completes with outcome 'done') is entirely untested, including the guard against re-handling the same outcome id.
- night: 2026-09-16 | check=tests | chunk=src/app | file=src/app/features/plans/views/chunk-detail.tsx | line=1 | commit=cb73cd1ec0cd7b9c53b0f70d057b0ee93cadf628 | severity=normal | ChunkDetail (the new /findings/chunk/$chunk view, including its 'Fix all' launch and per-finding dismiss handling) has no test file.
- night: 2026-09-16 | check=tests | chunk=src/app | file=src/app/server/routes/content/ideas.ts | line=370 | commit=cb73cd1ec0cd7b9c53b0f70d057b0ee93cadf628 | severity=normal | The new POST /api/night-findings/staleness route has no test coverage in ideas.test.ts, so its 400 validation branch and the hasFileChangedSince success/failure (catch-to-true) paths are unverified.
- night: 2026-09-16 | check=tests | chunk=src/app | file=src/app/hooks/use-route-selection.ts | line=100 | commit=cb73cd1ec0cd7b9c53b0f70d057b0ee93cadf628 | severity=normal | useActiveNightFinding, chunkRouteParam, and useActiveNightChunk are new exports with no corresponding cases added to use-route-selection.test.ts, leaving the chunk-key parsing/lookup and finding lookup-by-id logic untested.
- night: 2026-09-16 | check=dead-code | chunk=src/core | file=src/core/night-findings.ts | line=5 | commit=cb73cd1ec0cd7b9c53b0f70d057b0ee93cadf628 | severity=normal | SEVERITY_ORDER duplicates an identical constant already defined in src/app/features/hub/constants.ts:22, which is still used by hub-numbers-column.tsx instead of importing from core.
- night: 2026-09-16 | check=dead-code | chunk=src/core | file=src/core/night-findings.ts | line=22 | commit=cb73cd1ec0cd7b9c53b0f70d057b0ee93cadf628 | severity=normal | nightFindingTitle duplicates a near-identical local function in src/app/server/routes/content/ideas.ts:30 (same logic plus a length cap) rather than being reused there.
- night: 2026-09-16 | check=performance | chunk=src/core | file=src/core/night-health.ts | line=262 | commit=cb73cd1ec0cd7b9c53b0f70d057b0ee93cadf628 | severity=normal | selectNightChunks spawns a `git diff` subprocess (via hasFileChangedSince) for every chunk above the score threshold before sorting and slicing to maxChunks, so on every 5-minute night-shift tick it can fan out many concurrent git processes even though only maxChunks results are ever kept.
- night: 2026-09-16 | check=tests | chunk=src/core | file=src/core/night-findings.ts | line=17 | commit=cb73cd1ec0cd7b9c53b0f70d057b0ee93cadf628 | severity=normal | parseNightChunkKey has no test coverage anywhere in the repo — its round-trip with nightChunkKey and its null-return path for malformed keys are unverified.
- night: 2026-09-16 | check=tests | chunk=src/core | file=src/core/night-findings.ts | line=7 | commit=cb73cd1ec0cd7b9c53b0f70d057b0ee93cadf628 | severity=normal | nightFindingKey, nightChunkKey, nightFindingTitle, and sortedFindings are new exported functions with no unit tests; sortedFindings' severity/file comparator and nightFindingTitle's basename extraction are unverified by the suite.
tually executed by any test.
 the real token, so the timingSafeEqual comparison branch in isValidUpdateToken (src/cli/daemon-server.ts) is never actually executed by any test.
-mismatch short-circuit in isValidUpdateToken. No test supplies a wrong token of the same length as the real token, so the timingSafeEqual comparison branch in isValidUpdateToken (src/cli/daemon-server.ts) is never actually executed by any test.
tually executed by any test.
 the real token, so the timingSafeEqual comparison branch in isValidUpdateToken (src/cli/daemon-server.ts) is never actually executed by any test.
t.
- night: 2026-09-17 | check=bugs | chunk=src/app | file=src/app/hooks/use-route-selection.ts | line=116 | commit=14d7238d750aaa7520bdafbd548d790e98fc0227 | severity=high | useActiveNightChunk resolves a chunk only by chunk name (`g.findings.some((f) => f.chunk === chunkName)`), with no date component in the route anymore. If nightReport contains multiple date groups sharing the same chunk name (e.g. the same folder chunk appears in more than one night's report, which is the normal case), this always returns the first matching group's findings, silently showing the wrong night's findings for that chunk.
o openFindings can count findings whose source file already changed since the finding's commit, which readNightFindings would have excluded as resolved.
 resolved.
as resolved.
ightFindings would have excluded as resolved.
 resolved.
as resolved.
as resolved.
- night: 2026-09-18 | check=tests | chunk=src/app | file=src/app/hooks/use-route-selection.ts | line=111 | commit=f1780a0ceaa7f7a1598f8ca9caa0d4aae2a5914f | severity=normal | useActiveNightChunk's new tie-breaking logic (picking the most-recent night-report group by date when the same chunk appears in multiple groups) has no test coverage; use-route-selection.test.ts only tests the pure helper functions (resolveByIdOrTitle, entityLink, entityRouteParam), not this hook.
- night: 2026-09-18 | check=performance | chunk=src/cli | file=src/cli/daemon-lifecycle.ts | line=390 | commit=f1780a0ceaa7f7a1598f8ca9caa0d4aae2a5914f | severity=normal | In `runLogs`'s follow mode, the poll loop re-reads the entire daemon.log file from disk every 200ms via `readFile(logPath, 'utf-8')` and only uses the tail past `printedLength`, instead of tailing new bytes via a file descriptor/position. As the daemon log grows over a long uptime, each 200ms tick becomes an O(file size) full read+decode, so the polling cost grows unboundedly with the log's lifetime.
- night: 2026-09-18 | check=tests | chunk=src/cli | file=src/cli/night-command.ts | line=106 | commit=f1780a0ceaa7f7a1598f8ca9caa0d4aae2a5914f | severity=normal | The new 'open-findings' gate-reason label has no test exercising it; night-command.test.ts only covers the 'task-running' reason, so a typo or removal of this key would go undetected.
- night: 2026-09-21 | check=bugs | chunk=src/app | file=src/app/stores/slices/diff-slice.ts | line=68 | commit=bcc39b4a64a23b361f91942247a3a0f8d2d1280b | severity=normal | loadGitLog is built with loadSlice and no fallback, so if fetchGitLog(0) rejects, the catch branch applies nothing and gitLogCommits stays null forever. git-page.tsx shows a spinner whenever gitLogCommits === null, so a failed initial load leaves the user with an infinite spinner and no error/retry.
- night: 2026-09-21 | check=bugs | chunk=src/app | file=src/app/stores/slices/diff-slice.ts | line=78 | commit=bcc39b4a64a23b361f91942247a3a0f8d2d1280b | severity=normal | loadMoreGitLog awaits fetchGitLog in a try/finally with no catch; if the request rejects, the loading flag still resets but the error becomes an unhandled rejection from the 'Show older' button's onClick instead of being surfaced to the user.
- night: 2026-09-21 | check=tests | chunk=src/app | file=src/app/server/routes/git.ts | line=341 | commit=bcc39b4a64a23b361f91942247a3a0f8d2d1280b | severity=normal | The new GET /api/git/log endpoint (including its skip-param NaN fallback logic) has no test; src/app/server/routes has no git.test.ts file at all, unlike sibling route files which are each covered.
- night: 2026-09-21 | check=tests | chunk=src/app | file=src/app/stores/slices/diff-slice.ts | line=66 | commit=bcc39b4a64a23b361f91942247a3a0f8d2d1280b | severity=normal | loadGitLog and loadMoreGitLog (including the pagination-append logic and the guard against concurrent/duplicate loads) are untested; there is no diff-slice.test.ts file.
- night: 2026-09-21 | check=tests | chunk=src/app | file=src/app/features/roadmap/helpers/roadmap-filters.ts | line=17 | commit=bcc39b4a64a23b361f91942247a3a0f8d2d1280b | severity=normal | itemStatuses and matchesStatusFilter were changed to read item.ideas instead of item.links (a behavioral change), but there is no test file for roadmap-filters.ts to verify the new status-filter behavior.
